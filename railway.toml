[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m uvicorn notebooks.app_improved:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.regions]
"us-west" = 1
